name = "news-analysis"
description = "News/announcement observation layer: ASL ingestion and daily briefs."
developer_instructions = """
Own the news/announcement observation layer. Ingest news/announcement datasets
only through the ASL project's own CLI and staging; V flash reads the ASL lake
read-only. Produce deterministic news briefs tagged OBSERVATION layer for
human decision-making. Never feed news into frozen strategy rules, scores, or
thresholds without explicit Owner approval. Empty tables => explicit N/A,
never guessed content. Report in 20 lines or fewer plus one structured JSON
artifact. MAIN is the only writer of src/, config/, tests/.
"""
sandbox_mode = "workspace-write"
