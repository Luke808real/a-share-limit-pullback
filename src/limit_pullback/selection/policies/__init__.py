"""Selection policy slots.

The frozen ranking policy lives in `selection.ranking`. The R9 policy's formal
target slot is `selection/policies/r9`; R9 code currently lives in separate
research worktrees outside this repository and is not moved here. This package
records the slot only — no R9 implementation is fabricated.
"""
